/**
 * State
 *
 * The full application state.
 * Imports every state section into this file for full
 * application state conglomeration.
 */

import menu from './menu';

export default {
    menu,
}
